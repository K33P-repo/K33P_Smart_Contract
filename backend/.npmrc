# .npmrc file for Render deployment

# Use the official npm registry
registry=https://registry.npmjs.org/

# Allow running npm as root (needed for some cloud environments)
unsafe-perm=true

# Save exact versions to avoid unexpected updates
save-exact=true

# Increase network timeout for slow connections
network-timeout=60000
import { C } from "lucid-cardano/esm/src/core/mod.js";
import { Transaction, TxHash } from "lucid-cardano/esm/src/types/mod.js";
import { Lucid } from "lucid-cardano/esm/src/lucid/lucid.js";
export declare class TxSigned {
    txSigned: C.Transaction;
    private lucid;
    constructor(lucid: Lucid, tx: C.Transaction);
    submit(): Promise<TxHash>;
    /** Returns the transaction in Hex encoded Cbor. */
    toString(): Transaction;
    /** Return the transaction hash. */
    toHash(): TxHash;
}
